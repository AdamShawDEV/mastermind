const NUM_COLORS = 4;
const MAX_NUM_ROWS = 8;

const GAME_STATE = {
    PLAYING: 'playing',
    WON: 'won',
    LOST: 'lost',
}

const COLORS = {
  black: '#000',
  white: '#fff',
  red: '#f00',
  green: '#0f0',
  blue: '#00f',
  yellow: '#ff0',
}

export { NUM_COLORS, MAX_NUM_ROWS, COLORS, GAME_STATE };