const NUM_ROWS = 10;
const NUM_COLS = 4;

const GAME_STATE = {
    PLAYING: 'playing',
    WON: 'won',
    LOST: 'lost',
}

const COLORS = {
  black: '#000',
  white: '#fff',
  red: '#f00',
  green: '#0f0',
  blue: '#00f',
  yellow: '#ff0',
}

export { NUM_ROWS, NUM_COLS, COLORS, GAME_STATE };